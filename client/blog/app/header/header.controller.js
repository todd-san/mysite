angular.module('blogApp')
    .controller('headerViewController',
        ['$log', function($log){
            // $log.info('Called into categoryViewController');
            var self = this;

    }]);
